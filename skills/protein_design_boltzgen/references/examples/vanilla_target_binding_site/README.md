# Vanilla Target Binding Site Example

This folder contains working reference inputs copied from the preset example:

- `beetletert.yaml` (design spec)
- `5cqg.cif` (target structure)

Use these files together when running the API workflow for a quick smoke test.
